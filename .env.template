# Environment Configuration
# Copy this file to .env and fill in your values

# Flask Secret Key (generate a random 32-character string)
FLASK_SECRET_KEY=your-random-32-character-secret-key

# Debug Mode (set to False for production)
FLASK_DEBUG=True

# Optional: Custom WordPress configuration path
# WP_CONFIG_PATH=custom/path/to/wordpress_config.json

# NOTE: OpenAI API key is now entered through the web interface
# and stored securely in browser sessions (not environment variables)